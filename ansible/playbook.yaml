
---

- name: Setup CI/CD servers
  hosts: ci_nodes
  become: true
  roles:
    - docker
    - jenkins
    - vault

- name: Setup application servers
  hosts: app_nodes
  become: true
  roles:
    - docker